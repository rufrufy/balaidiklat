# Project Context
<!-- session: 2026-08-11T01:14:57.430Z | count: 2 | last-prune: 2026-08-11 | last-update: 2026-08-11T01:55:05.179Z | content-hash: 747c4c27 | stale-sessions: 0 -->
> Auto-maintained by AI. You can edit this file freely.
> Archived entries: see .opencode-context-archive.md
> Detailed handoff index: .opencode-jce/context/session.md
> Last updated: 2026-08-11

## Stack
- (auto-detect from project files)

## Architecture Decisions
- Webhook payload kirimdev = Meta Cloud API shape: entry[].changes[].value.messages[].
- Butuh phone_number_id: dari GET /v1/accounts (field phone_number_id per row).
- KirimChatService + KirimChatWebhookController extract* methods WAJIB rombak untuk Meta payload shape.

## Conventions
- (none yet)

## Current Status
- Migrasi kirimdev SELESAI code-side: config+service+extract methods+shouldProcessChatbot.
- USER HARUS ISI .env KIRIMCHAT_API_KEY dengan kdv_live_ baru (key lama kc_live invalid).
- PHONE_NUMBER_ID=978420005347845 sudah terisi dari payload webhook.
- KirimChatService: POST {base_url}/{phone_number_id}/messages body Meta Cloud API + normalizePhone().
- Migrasi provider kirim.chat -> kirimdev selesai code-side. Inbound webhook berfungsi (extract methods + shouldProcessChatbot tambah path kirimdev). Outbound KirimChatService rombak ke Meta Cloud API
- Last touched: config/services.php, app/Services/KirimChatService.php, app/Http/Controllers/KirimChatWebhookController.php, app/Console/Commands/TestKirimdevSend.php, .env, .env.example.
- Verified: php -l semua file bersih.
- Verified: php artisan config:clear + tinker: base_url=kirimdev phone_id=978420005347845 api_key_empty=true (local).
- Verified: php artisan list kirimdev: command terdaftar.
- Verified: php artisan kirimdev:test: tunjukkan api_key empty.
- Blocker: User lapor outbound production tetap gagal padahal key sudah isi di dokploy. Perlu user jalankan `php artisan kirimdev:test 628xxx` di server dokploy untuk lihat error code/body persis (401/404/422). Tanpa error response, tidak bisa konfirmasi root cause pasti..
- Next: User jalankan di dokploy: php artisan config:clear; php artisan kirimdev:test 6287738350324.
FIXED: dashboard.blade.php git merge conflict markers (HEAD/ac396d04) resolved. Conflict caused duplicate section#whatsapp, duplicate script tag, Blade leaking into JS, breaking section switching.
Last touched: resources/views/admin/dashboard.blade.php
Verified: php -l clean, blade compile OK, 7 unique sections, 1 script tag, 1 template, no conflict markers
FIXED: dashboard.blade.php duplikat function reindexRows (sisa merge conflict). Brace balance 52/52, JS valid (node --check pass), view cache cleared.
Verified: php -l clean, blade compile OK, node --check pass, view:clear + config:clear run
FIXED: (1) Mojibake dashboard.blade.php - BOM + double-encoded chars (star/bullet/arrow/em-dash) dirapikan.
FIXED: (2) Hardcode ruleActionOptions mismatch - buat method actionOptions() di AdminChatbotRuleController (26 actions), pass ke view via AdminDashboardController, hapus hardcode dari blade.
Last touched: AdminChatbotRuleController, AdminDashboardController, dashboard.blade.php
Verified: php -l clean, view:cache OK, actionOptions 26 keys sinkron controller+view

## Important Notes
- (none yet)

## Related Projects
- (none — add related projects as: - <path>: "<description>")

# Project Context
<!-- session: 2026-07-06T17:58:57.076Z | count: 1 | last-prune: 2026-07-06 | last-update: 2026-07-07T01:58:12.227Z | content-hash: d40e79bb | stale-sessions: 0 -->
> Auto-maintained by AI. You can edit this file freely.
> Detailed handoff index: .opencode-jce/context/session.md
> Last updated: 2026-07-07

## Stack
- (auto-detect from project files)

## Architecture Decisions
- Webhook payload kirimdev = Meta Cloud API shape: entry[].changes[].value.messages[].
- Butuh phone_number_id: dari GET /v1/accounts (field phone_number_id per row).
- KirimChatService + KirimChatWebhookController extract* methods WAJIB rombak untuk Meta payload shape.

## Conventions
- (none yet)

## Current Status
- Migrasi kirimdev SELESAI code-side: config+service+extract methods+shouldProcessChatbot.
- USER HARUS ISI .env KIRIMCHAT_API_KEY dengan kdv_live_ baru (key lama kc_live invalid).
- PHONE_NUMBER_ID=978420005347845 sudah terisi dari payload webhook.
- KirimChatService: POST {base_url}/{phone_number_id}/messages body Meta Cloud API + normalizePhone().
- Migrasi provider kirim.chat -> kirimdev selesai code-side. Inbound webhook berfungsi (extract methods + shouldProcessChatbot tambah path kirimdev). Outbound KirimChatService rombak ke Meta Cloud API
- Last touched: config/services.php, app/Services/KirimChatService.php, app/Http/Controllers/KirimChatWebhookController.php, app/Console/Commands/TestKirimdevSend.php, .env, .env.example.
- Verified: php -l semua file bersih.
- Verified: php artisan config:clear + tinker: base_url=kirimdev phone_id=978420005347845 api_key_empty=true (local).
- Verified: php artisan list kirimdev: command terdaftar.
- Verified: php artisan kirimdev:test: tunjukkan api_key empty.
- Blocker: User lapor outbound production tetap gagal padahal key sudah isi di dokploy. Perlu user jalankan `php artisan kirimdev:test 628xxx` di server dokploy untuk lihat error code/body persis (401/404/422). Tanpa error response, tidak bisa konfirmasi root cause pasti..
- Next: User jalankan di dokploy: php artisan config:clear; php artisan kirimdev:test 6287738350324.
FIXED: dashboard.blade.php git merge conflict markers (HEAD/ac396d04) resolved. Conflict caused duplicate section#whatsapp, duplicate script tag, Blade leaking into JS, breaking section switching.
Last touched: resources/views/admin/dashboard.blade.php
Verified: php -l clean, blade compile OK, 7 unique sections, 1 script tag, 1 template, no conflict markers
FIXED: dashboard.blade.php duplikat function reindexRows (sisa merge conflict). Brace balance 52/52, JS valid (node --check pass), view cache cleared.
Verified: php -l clean, blade compile OK, node --check pass, view:clear + config:clear run
FIXED: (1) Mojibake dashboard.blade.php - BOM + double-encoded chars (star/bullet/arrow/em-dash) dirapikan.
FIXED: (2) Hardcode ruleActionOptions mismatch - buat method actionOptions() di AdminChatbotRuleController (26 actions), pass ke view via AdminDashboardController, hapus hardcode dari blade.
Last touched: AdminChatbotRuleController, AdminDashboardController, dashboard.blade.php
Verified: php -l clean, view:cache OK, actionOptions 26 keys sinkron controller+view

## Important Notes
- (none yet)
- Last touched files: app/Http/Controllers/KirimChatWebhookController.php, database/seeders/ChatbotRuleSeeder.php, tests/Feature/ExampleTest.php.
- Last verified: php -l app/Http/Controllers/KirimChatWebhookController.php: passed.
- Last verified: php -l database/seeders/ChatbotRuleSeeder.php: passed.
- Last verified: php -l tests/Feature/ExampleTest.php: passed.
- Last verified: vendor\bin\pint --test changed files: passed.
- Last verified: php artisan test --filter=sapa_balai_payment_proof_upload_marks_paid: blocked by missing pdo_sqlite PHP extension.
- Android project context detected.
- Last touched files: app/Http/Controllers/KirimChatWebhookController.php.
- Last verified: vendor\bin\pint --test app/Http/Controllers/KirimChatWebhookController.php: passed.
- Last verified: php artisan test --filter=sapa_balai_payment_proof_upload_marks_paid: still blocked by missing pdo_sqlite PHP extension.

## Related Projects
- (none — add related projects as: - <path>: "<description>")
